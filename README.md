# Sloving-XOR-problem-using-DNN
This project demonstrates how to solve the XOR (Exclusive OR) problem using a Deep Neural Network (DNN). The XOR problem is a classic example used to show why a neural network needs hidden layers to learn non-linear relationships
